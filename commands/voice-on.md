---
description: Voice mode ON for this session (voicebridge) - talk hands-free, replies spoken
allowed-tools: Bash(vb talkd:*), Bash(/opt/homebrew/bin/vb talkd:*)
---

Run `/opt/homebrew/bin/vb talkd on` with the Bash tool now.

If it succeeded, tell the user in one short sentence: voice mode is on for
this session; keep this window focused and just talk, replies will be
spoken aloud; say "stop listening" or use /voice-off to end. If the command
printed an ERROR, relay that error instead and do nothing else.
